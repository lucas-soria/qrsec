pipeline {

    agent any

    stages {

        stage('build front') {
            steps {
                echo 'build front'
            }
        }

        stage('build back') {
            steps {
                echo 'build back'
            }
        }

        stage('deploy') {
            steps {
                echo 'deploying'
            }
        }

    }
}
